# 修仙挂机玩法与 UI 重构 TODO

## 已落地

- [x] 移除 Phaser 战斗渲染链，改为 Vue + Canvas 2D，并保留现有回合制 `BattleRuntime`。
- [x] 战斗采用上方敌方、下方我方的竖屏布局，接入 9:16 人物立绘、像素角色素材和元素技能演出。
- [x] Canvas 支持火焰、雷链、风刃、冰锥、水浪、藤蔓、地裂、剑阵、治疗回环、受击/治疗数值。
- [x] 故事页改为沉浸式独立路由，正文保留作者段落并在面板内部滚动，避免按字数强行拆句。
- [x] 故事角色优先使用 `portraits-9x16` 素材。
- [x] 物品、装备、商店统一经过 `ItemArt`，优先三方贴图，缺失时回退到统一图标。
- [x] 世界 tick 生成结构化日志、履历、天气影响和事件通知。
- [x] 天气在首页显示实际影响，且已经参与修炼、历练战斗、地图探索、商店和 NPC 行为。
- [x] 战斗掉落先固定结果，再按背包容量进入背包或持久化到待领取奖励；履历区分已领取和暂存。
- [x] 战斗旧面板链已移除，改为暗色全屏战场、圆形头像节点、行动序列和统一指令台；普通攻击、元素技能、暴击、治疗和受击均有粒子/冲击波反馈。
- [x] 地图区域、界域、宗门和人物的新开放状态会生成 `WorldUnlock`、公告和世界履历。

## P0：下一轮必须完成

- [x] 为待领取奖励增加批量领取和容量预估，避免逐件点击造成重复操作。
- [x] 为每次奇遇、故事选择和战斗结果建立统一的 `WorldEffect` 事务记录，保证人物履历可以展示“变强、受伤、换宗门、获得技能”等具体后果。
- [x] 把地图开放条件从“境界/征服”扩展为“事件、邻接路径、主线节点、宗门势力”四类来源，并在解锁公告中展示来源。
- [x] 统一 NPC、伙伴、宗门和技能池的定义 schema，补齐随机生成角色的技能稀有度、前置条件和组织归属。
- [x] 清理旧的测试入口、主题展示入口和生产环境遗留按钮，所有功能入口必须连接真实 store/action。

## P1：玩法闭环

- [x] 天气：补充“赶路”独立行动，包含体力、耗时、路线风险、天气修正和中断事件。
- [x] 奇遇：每个选择必须返回资源、关系、状态、旗标或路线变化，并写入主角与相关 NPC 的履历。
- [x] NPC 自驱动：每个 tick 维护位置、目标、境界、伤势、宗门/组织、关系和关键事件记忆；主线只提供约束，不直接覆盖随机结果。
- [x] 故事：支持主线节点与世界事件互相引用，人物在世界中晋升、结仇、加入组织后，后续故事读取真实状态。
- [x] 伙伴：区分战斗定位和非战斗关系技能，至少覆盖治疗、护盾、控制、增益、召唤五类效果。
- [x] 战斗：补齐目标选择、群体技能、状态图标、行动预告、死亡/撤退后果和战斗回放；世界 Boss 使用独立奖励池与阶段机制。
- [x] 奖励：建立普通掉落、区域掉落、天气掉落、事件掉落、Boss 掉落和故事纪念物命名表，名称和描述随主线扩展。
- [x] 宗门与地图：控制区域、宗门战局、商店库存、NPC 位置和事件池互相影响，解锁用公告进入玩家可回看的世界记录。

## P2：体验与内容

- [x] 首页只保留修炼、历练、世界脉动和当前天象四个核心层级，减少重复入口。
- [x] normal / half-immersive / immersive / battle 四种布局继续收敛为路由元数据驱动，避免页面自行显示顶部栏或底部 tab。
- [x] 统一图标命名和尺寸 token，逐步移除 emoji 作为功能图标的回退方案。
- [x] 为背包、商店、宗门设施和地图卡片补齐图片比例、裁切和空状态测试。
- [x] 增加移动端 390x844、430x932 的截图回归，覆盖首页、地图、宗门、伙伴、物品、商店、故事和战斗。

截图回归命令：`npm run ui:regression`。该命令连接本地 Chrome CDP，输出截图与 `report.json` 到 `/tmp/woyu-ui-regression`；Image2 透明图标仍需在配置生成环境中落盘，未生成前由语义 Lucide 组件兜底。

## 并行拆分

- `ui-shell`：路由布局、HUD、tab、沉浸式故事和组件尺寸。
- `world-sim`：天气、赶路、奇遇、NPC tick、解锁和履历事务。
- `battle-canvas`：战斗 HUD、Canvas VFX、技能表现、回放和奖励结算。
- `content-schema`：技能池、伙伴技能、掉落表、Boss 奖励和故事事件数据。
- `asset-audit`：9:16 立绘、物品贴图、图标映射、裁切比例和缺失素材清单。

模块之间只通过 `WorldTickResult`、`WorldEventBus`、战斗事件总线和显式 store action 通信；不要直接修改其他模块的内部 ref。

## 资源边界

Canvas 可以可靠完成粒子、雷链、火焰路径、风刃、剑光、受击闪烁、数值飘字和简单位移。当前仓库没有完整的逐帧人物动作、骨骼动画或技能序列帧，因此不能把静态立绘伪装成真正的动作立绘；新增多帧动作需要补充授权素材后再接入。

## 验证命令

```bash
npm run build
npm run assets:validate
npm run runtime:validate
npm run story:validate
git diff --check
```
