/**
 * 故事事件总线 — 存根实现
 */
export const storyEventBus = {
  emit: (event: unknown) => { /* stub */ },
  on: (event: string, handler: unknown) => { /* stub */ },
  off: (event: string, handler: unknown) => { /* stub */ }
}
